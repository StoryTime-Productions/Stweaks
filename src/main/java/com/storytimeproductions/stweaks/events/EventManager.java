package com.storytimeproductions.stweaks.events;

import org.bukkit.plugin.java.JavaPlugin;

/**
 * Manages events related to the plugin.
 *
 * <p>This class provides methods to initialize and set up any gallery or event logic required by
 * the plugin. It is used to organize and handle plugin events in a centralized way.
 */
public class EventManager {

  /**
   * Initializes the event manager for the plugin.
   *
   * <p>This method is called to load or set up any necessary event handling logic, such as
   * registering events or configuring gallery-related functionality. It must be called to ensure
   * proper event management within the plugin.
   *
   * @param plugin The plugin instance used to set up the event system.
   */
  public static void init(JavaPlugin plugin) {
    // Load or setup gallery/event logic
  }
}
